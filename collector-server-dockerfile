FROM golang:1.25.3

RUN apt-get install && apt-get install -y git

WORKDIR /app

COPY go.mod go.sum ./

RUN go mod download

COPY packages ./packages/
COPY cmd/collector-server/main.go .

RUN go build -o collector-server main.go

CMD [ "./collector-server" ]
